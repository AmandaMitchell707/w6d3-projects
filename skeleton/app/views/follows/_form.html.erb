<% if current_user.follows?(@user) %>
  <% is_follow = 'followed' %>
<% else %>
  <% is_follow = 'unfollowed' %>
<% end %>

<button class="follow-toggle" type="button" data-user-id="<%= @user.id %>" data-initial-follow-state="<%= is_follow %>" name="button"></button>

